export interface ProductItem {
    id: number,
    name: string,
    teaser: string,
    price: string,
    imageUrl: string,
    stockText: string,
    stockClass: string,
    description: string,
    slug: string
}